function Main(input) {
  const text = input.map(x => +x);
  const N = text[0];

  const amari = Math.pow(10,9) + 7
  let arr =[];

  for 
  for (let x = 0; x <= 9; x++) {
    arr.
  }
}

// Main(require("fs").readFileSync("sample.txt", "utf8"));  // テスト用
Main(require("fs").readFileSync("/dev/stdin", "utf8"));   // 提出用